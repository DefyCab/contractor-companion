export function calculatePaint() {
  // indoors

  //assumption 2.70
  const ceilingHeight = 0;
  const wallLength = 0;

  const numberOfWalls = 0;
  const numberOfWindows = 0;
  const numberOfDoors = 0;
  //assumption 2
  const numberOfCoats = 0;

  const surface = ["drywall", "concrete", "wood"];
}
